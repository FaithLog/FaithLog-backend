# 2026-06-19 FaithLog Dev Log

## Issue #37 Poll Template And Poll Create

- Branch: `feat/37-poll-template-poll-create`
- Worktree: `/Users/josephuk77/.codex/worktrees/d1cf/FaithLog`
- Scope: coffee catalog seed, poll template management, poll creation, REST Docs.
- Seed source decision: official Compose Coffee web access was blocked, and the user approved using the provided 2026 Compose Coffee menu/price list for Issue #37.
- Reusable seed files:
  - `src/main/resources/seed/compose-coffee-menu-2026.csv`
  - `docs/seed/compose-coffee-menu-2026.md`
- Important menu split: `AMERICANO_HOT` is 1,500 KRW and `AMERICANO_ICE` is 1,800 KRW.

## TDD Evidence

- Failing test first: `./gradlew test --tests 'com.faithlog.poll.application.PollServiceTest'`
- Initial result: `compileTestJava` failed with missing poll domain/service/repository types, 97 errors.
- Fix path: added poll domain entities, coffee CSV seeder, default coffee template provisioner, application services, controllers, DTOs, and REST Docs tests.

## Verification

- `./gradlew test --tests 'com.faithlog.poll.application.PollServiceTest'`: success.
- `./gradlew test --tests 'com.faithlog.poll.presentation.PollApiRestDocsTest'`: success.
- `./gradlew test --rerun-tasks`: success, 144 tests / 0 failures / 0 errors / 0 skipped.
- `./gradlew build`: success.
- `./gradlew asciidoctor`: first sandbox run failed on Gradle wrapper lock under `~/.gradle`; elevated rerun succeeded.
- Docker: `docker compose build app` success, `docker compose up -d postgres redis app` success, elevated `curl -fsS http://localhost:8080/api/v1/health` returned `UP`, `docker compose down` completed.
- REST Docs snippet groups: 65.

## Notes

- Controller methods return DTO responses, not Entity objects.
- Swagger documentation annotations were not added.
- #38 response/result/comment and #39 coffee charge automation were not implemented.
