# Issue #192 poll settlement before baseline

This tooling measures the approved `origin/develop` before baseline only. It must not be run until the PM has reviewed this contract and explicitly granted the #192 measurement slot.

## Authoritative current-develop before bundle

The PM completed the strict four-mode bundle at `build/reports/k6/poll-settlement/bundles/EXEC192_BEFORE_BUNDLE_O/`. It binds source commit `355f79df5b2e47636b7d1a17dea029da6c93c62d`, target SHA-256 `8a35ba2b2be146c40d89c00670fe16a62548e9a0e7d4501e347d7e05dee2ca80`, protocol `faithlog-192-single-mode-v1`, and a fresh 1,000 ACTIVE / 34 poll / 34,000 response fixture per mode. `_CS_O`, `_MS_O`, `_CC_O`, and `_MC_O` each passed SHA continuity and strict evidence validation. All failure rates are zero, while classification remains `conditional-boundary-only`, `accepted=false`, and `automaticAdoption=false` because continuous exclusivity is not machine-proven.

| mode | count | p50 ms | p95 ms | p99 ms | max ms | throughput req/s |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| coffee-sequential | 10 | 890.6895 | 1117.2471 | 1168.37502 | 1181.157 | 1.0649627 |
| meal-sequential | 10 | 1146.035 | 1294.67735 | 1303.83467 | 1306.124 | 0.8567512 |
| coffee-concurrent | 5 | 2732.844 | 4330.2564 | 4474.45768 | 4510.508 | 1.1084017 |
| meal-concurrent | 5 | 3019.871 | 6293.2634 | 6762.88068 | 6880.285 | 0.7265330 |

`pg_stat_statements` was unavailable, so DB/table counters and container resource evidence are supporting evidence rather than per-query attribution. These numbers are the approved current-develop before reference for final integration comparison, not an automatically adopted performance achievement.

## Production settlement batch optimization

The before bundle's supporting table counters match the production loop structure. COFFEE sequential recorded `payment_accounts.seq_scan=9009`, `charge_items.idx_scan=9000`, and `users.idx_scan=9020`; MEAL sequential recorded `10020`, `10000`, and `10032`. The concurrent modes recorded the same pattern at their smaller request counts: COFFEE `4004/4000/4011` and MEAL `5010/5000/5017`. Because `pg_stat_statements` was unavailable, these are supporting correlations, not per-statement attribution.

The production change keeps each settlement in its existing transaction and preserves poll/duty/account ownership checks, amount calculation, duplicate prevention, terminal charge handling, API responses, and archive visibility. The settlement services now hand one charge-command collection to billing. Billing validates the selected account once per collection, loads existing source charges with one pessimistically locked source-ID set query, updates only existing UNPAID COFFEE charges, leaves terminal COFFEE charges unchanged, rejects any existing MEAL charge, and submits new entities through one repository collection-save boundary.

This does not claim JDBC insert batching. `ChargeItem` still uses identity IDs, and every required charge row must still be inserted. The supported optimization claim is removal of per-charge account and existing-charge repository lookups. No controller/DTO/API, Flyway migration, dependency, or index changed. Actual after latency and counter deltas remain reserved for the final PM integration run.

TDD first failed at `compileTestJava` with eight missing batch-contract symbols. The minimal implementation then passed the four new batch tests, the expanded 134-test billing/poll/#200/#201 focused set, the full Gradle suite (`556` tests, `0` failures, `0` errors, `3` skipped), Gradle build, asciidoctor, and all 77 issue-local static/fake contracts.

Issue #194 should inspect `EXPLAIN (ANALYZE, BUFFERS)` for the new locked bulk lookup and evaluate a `charge_items (campus_id, payment_category, source_type, source_id)` index. The existing unique index places `user_id` before category/source columns, so it may not serve this query shape efficiently. #194 should also inspect the existing `poll_options` `poll_id/order` lookup before deciding whether a `(poll_id, sort_order)` index is warranted. `payment_accounts` already has a primary-key lookup path; its before sequential scans may simply be the planner's choice for the small fixture relation and are not evidence for another index by themselves.

## Immutable target

`target-contract.json` binds the run to the PM-approved app/PostgreSQL/Redis full container IDs, images, start times, Compose identities/config hashes, published ports, database/postmaster identity, Redis run ID, base URL, source commit, Flyway V11, resource sampling interval, and per-mode expected charge writes. `run-baseline.sh` aborts on identity drift, an occupied cross-issue lock, or another `k6 run` process. It never builds, restarts, stops, removes, or prunes Docker resources.

The credential is supplied only as the runtime environment variable `PERF_PASSWORD`. It is used to create and authenticate the fixture actor and is never written to a report or repository file. Each measured phase uses a fresh JWT prepared before the DB/resource boundary in a mode-0600 temporary file, which is deleted immediately after that measured k6 process.

## Required fresh identifiers

- `MODE`: one exact mode only: `coffee-sequential`, `meal-sequential`, `coffee-concurrent`, or `meal-concurrent`; there is no default, alias, or `all`
- `PERF_DATASET_ID`: fixture namespace, for example `PERFORMANCE_192_BEFORE_20260715_B`
- `PERF_FIXTURE_RUN_ID`: immutable fixture manifest namespace, for example `BEFORE_20260715_B`
- `PERF_EXECUTION_RUN_ID`: immutable measurement report namespace, for example `EXEC192_BEFORE_20260715_B`
- `REPORT_ROOT`: required absolute or relative report root; there is no runtime default
- `BASE_URL`: must be exactly `http://127.0.0.1:28080`
- `TOKEN_EXPIRY_SAFETY_SECONDS`: required positive JWT coverage margin; the approved before run uses exactly `120`
- `MAINTENANCE_POLL_INTERVAL_SECONDS`: required and exactly `5`
- `MAINTENANCE_QUIET_SECONDS`: required and exactly `30`
- `MAINTENANCE_TIMEOUT_SECONDS`: required and exactly `180`

Never reuse an identifier or accumulated fixture state for a comparison run.

The approved measured duration contract is sequential `maxDuration=27m` (1,620 seconds) and concurrent `maxDuration=14m` (840 seconds). With `TOKEN_EXPIRY_SAFETY_SECONDS=120`, a newly issued 1,800-second JWT has 60 seconds of sequential preparation headroom. The token gate requires the remaining lifetime at validation time to cover the exact duration plus safety boundary and rejects a one-second shortfall.

## Read and write scope

Preflight reads Docker metadata/stats, `/api/v1/health`, `/api-docs`, Flyway history, selected PostgreSQL settings/statistics, and the canonical lock path `/tmp/faithlog-performance-faithlog-frontend-latest.lock`.

Fixture setup writes only rows namespaced by the fresh dataset ID in `users`, `campuses`, `campus_members`, `campus_duty_assignments`, `payment_accounts`, `polls`, `poll_options`, `poll_responses`, and `poll_response_options`, then runs `ANALYZE` for the involved tables. It creates 1,000 ACTIVE members, two ACTIVE COFFEE duties, creator-owned COFFEE polls/accounts, account-neutral template behavior, and the approved COFFEE/MEAL response cardinality.

The measured production endpoints write settlement state to `polls`, `charge_items`, `meal_poll_settlements`, and `meal_poll_charge_groups`. The runner never resets, installs, or configures `pg_stat_statements`; it uses strict before/after cumulative snapshots and composite identity deltas, or a strict both-unavailable record. Post-checks read all fixture rows plus the member charge API using `page=0&size=10&includeArchived=false`, and verify no notification side effects. Reports are written only beneath `REPORT_ROOT`.

## Fixed execution order

Each runner invocation handles exactly one required `MODE`. It re-runs static checks, validates the immutable server/API/Flyway target, acquires the common performance lock, seeds and analyzes a new full fixture, and then executes only the selected mode's readiness, excluded warmup, measured requests, correctness, final continuity, and conditional summary. Nonselected readiness, warmup, DB/resource/k6 evidence, settlement writes, and summaries are forbidden.

The PM runs four completely separate fresh dataset/fixture/execution identities in this order: COFFEE sequential, MEAL sequential, COFFEE concurrent, then MEAL concurrent. Each selected readiness gate polls every 5 seconds and requires worker zero, exact 12-table maintenance stability, and sufficient `charge_items` autoanalyze headroom for 30 continuous seconds; it fails closed after 180 seconds. Actual loads remain strictly one at a time on the shared server.

`bundle-results.mjs` combines only four strict conditional summaries with the exact mode set, distinct run identities, the same source/target role/workload contract, contained regular JSON files, and matching SHA-256 descriptors. Parsed target and fixture contents must match each summary and case. The bundle preserves each mode's metrics separately, never synthesizes a combined latency, and remains `accepted=false` and `automaticAdoption=false`.

## Evidence classification

Runtime, DB activity, planner, maintenance, `pg_stat_statements`, and container resources are fail-closed at the captured boundaries. The common lock coordinates approved Codex runners, but there is no continuous observer proving that transient external read-only activity was absent between boundaries. Therefore a clean result is recorded as `evidenceIntegrity=validated`, `measurementStatus=conditional-boundary-only`, and `automaticAdoption=false`; the numeric summary is preserved, but `accepted:true` is never emitted. PM review remains required before the baseline can be used.

After the initial synchronous resource sample, the background sampler invokes the blocking `docker stats --no-stream` capture back-to-back while its marker exists. It does not add a sleep after each blocking capture. Marker removal is a successful sampler stop, while any capture failure remains non-zero and prevents validated classification. The strict `maxGapMs=3000` evidence gate is unchanged.

Measured-window resource coverage uses the latest sample at or before the measured start and the earliest sample at or after the measured end. Both selected boundary samples must be within `maxGapMs`; later valid final samples do not invalidate an already covered end boundary. Every sample still requires strict schema/identity, unique increasing timestamps, and consecutive gaps no greater than `maxGapMs`. A mode-validator failure writes its sanitized stage/reason to the rejected adoption record, and the runner skips the full summarizer so missing future-mode files cannot replace the first failure.

Docker renders `MemUsage` used/limit and `MemPerc` independently. The collector and evidence validator therefore share one exact display-precision contract: each rendered value becomes its half-display-unit rounding interval, and the possible used/limit ratio interval must overlap the rendered percentage interval. Canonical integer-safe byte representatives and display strings must still match exactly, memory percentages above 100 are rejected, and multi-core CPU percentages above 100 remain valid. A background capture/parser failure records the first sanitized stage such as `coffee-sequential-resource-sampler` with reason `resource-capture-failed`; later cleanup or capture failures cannot replace it.

The PM-only `_F` attempt completed COFFEE sequential warmup once and ten measured requests with zero failures. Its observed latency was avg `1085.800ms`, p50 `1019.432ms`, p95 `1394.072ms`, p99 `1525.923ms`, max `1558.886ms`, but the first background resource row was falsely rejected by the former point-estimate percentage check and no after evidence was produced. `_F` is rejected, preserved, never reused, and these observations are neither a baseline nor a performance achievement.

Fresh `_G` completed seeding and COFFEE sequential, then failed closed at the MEAL sequential DB pair because `users.autovacuum_count` changed `1 -> 2` and `lastAutovacuum` changed from `22:50:57Z` to `23:34:59Z`. Fresh `_H` failed closed at the COFFEE sequential DB pair because `poll_responses.autovacuum_count` changed `3 -> 4` and `lastAutovacuum` changed from `23:07:58Z` to `23:37:59Z`; `poll_response_options` also had `autovacuum_count=4` at the same time. No active autovacuum worker remained at the later audit boundary. Neither run produced an accepted or validated adoption record. `_G` and `_H` are rejected, preserved, and never reused.

These failures show that maintenance started after the fresh bulk seed and `ANALYZE` and entered a measured DB window. The existing validator correctly rejects that contamination. The user approved a post-seed gate with runtime-required exact values `5/30/180`: every read-only poll records the instance-wide active `autovacuum worker` count and exact `last_analyze`, `last_autoanalyze`, `last_vacuum`, `last_autovacuum`, `analyze_count`, `autoanalyze_count`, `vacuum_count`, and `autovacuum_count` snapshot for all 12 evidence tables. Any worker or field drift resets the quiet clock immediately; 29 seconds cannot pass, 30 seconds can, and 180 seconds without success fails before warmup or any measured DB/resource boundary. Consecutive observations must be at least five seconds apart; collector overhead may make them later but never earlier.

The machine evidence contains only case identity, the approved inputs, gate start, last `quietStartedAt`, finish, poll/reset counts, final status, and a sanitized reason. A passed record independently proves at least 30 seconds from the last quiet start to finish and at most 180 seconds for the whole gate. With zero resets, quiet start must equal gate start; after any reset it must be later. This prevents an overall 30-second duration from hiding a reset during the window. The gate adds no `VACUUM`/`ANALYZE`, configuration mutation, statistics reset, or extension change.

The PM-only fresh `_I` attempt passed the quiet gate from `00:27:43.472Z` to `00:28:14.226Z` with `quietStartedAt` equal to start, seven polls, zero resets, and exact `5/30/180` metadata. COFFEE sequential and MEAL sequential completed with zero measured failures, followed by five successful COFFEE concurrent requests. During the COFFEE concurrent measured window (`00:28:56.165Z` to `00:29:02.504Z`), natural `charge_items` autovacuum at `00:29:01.911Z` and autoanalyze at `00:29:02.139Z` overlapped the final approximately 0.6 seconds. Its before/after counters changed from `autoanalyze_count=4` / `autovacuum_count=2` to `5` / `3`. The runner correctly failed closed and produced no accepted or validated summary. `_I` is rejected, preserved, and never reused.

Rejected observations from `_I` are not a baseline or performance achievement: COFFEE sequential avg/p50/p95/p99/max were `949.660/936.207/1069.439/1087.285/1091.746ms`; MEAL sequential were `980.010/960.980/1088.337/1095.499/1097.290ms`; COFFEE concurrent were `3745.826/3811.780/6127.945/6295.797/6337.760ms`. The post-seed gate alone cannot prevent earlier modes from increasing `n_mod_since_analyze` until a later mode crosses the natural autoanalyze threshold. After `_I`, `charge_items` had `reltuples=162011` and `n_mod_since_analyze=2000`; observed settings were analyze threshold `50`, scale factor `0.1`, and naptime `1min`.

The user approved a selected-mode readiness gate for each fresh run. Before that run's warmup it reads `n_mod_since_analyze`, `reltuples`, global settings and table reloption overrides, worker state, and the 12-table maintenance snapshot. Expected warmup-plus-measured writes are exact and target-bound: COFFEE sequential `11000`, MEAL sequential `11000`, COFFEE concurrent `6000`, and MEAL concurrent `6000`. Quiet time advances only while `n_mod_since_analyze + expectedWrites` is strictly less than the smallest integer autoanalyze trigger count, workers are zero, and maintenance fields are stable. Insufficient headroom remains pending for natural maintenance and resets the quiet clock; the selected mode fails before warmup after 180 seconds.

Headroom uses decimal/BigInt rational arithmetic rather than JavaScript `Number`. Selected-mode evidence losslessly records `nModSinceAnalyze`, `reltuples`, effective base threshold, reduced rational scale factor, computed `triggerAt`, expected writes, projected modifications, and the sufficient flag. A reloption overrides its corresponding global value; disabled autovacuum/autoanalyze, unknown or negative relation statistics, malformed settings, unsafe numeric JSON, missing/mismatched selected mode, and forged headroom fail closed. The per-mode summarizer requires only the selected mode and rejects any foreign-mode artifact; the four-mode bundle then requires all four independent conditional summaries. No manual `VACUUM`/`ANALYZE`, configuration/reloption mutation, statistics reset, extension change, or Docker lifecycle action is performed.

The PM-only fresh `_J` completed the COFFEE sequential warmup and ten measured requests, then failed closed at its first DB-pair validation with `activity drift`. The initial and before boundaries contained five idle Hikari connections with PIDs `33948`, `34291`, `34496`, `34906`, and `35061`; the after boundary retained the same five-fold semantic identity but PID `33948` had naturally rotated to PID `35476` with backend start `01:00:03.539781Z`. `_J` produced no accepted or validated summary, is rejected and preserved, and is never reused. No observed number from it is a baseline or performance achievement.

Activity rows still retain strict unique PID, RFC3339 backend start, exact row schema, and `state=idle`, and raw evidence continues to preserve PID/start provenance. Boundary continuity compares a sorted multiset of database, user, application name, client address, and state, preserving multiplicity while excluding only PID and backend start. Thus normal idle-pool rotation is allowed, while count, semantic identity, state, null/schema, and duplicate-PID drift remain fail-closed. DB-pair failures preserve a sanitized stage such as `coffee-sequential-db-pair-validator` and reason such as `activity-drift`; boundary-only classification and `automaticAdoption=false` are unchanged.

The PM-only fresh `_K` identifiers were `PERFORMANCE_192_BEFORE_20260716_K`, `BEFORE_20260716_K`, and `EXEC192_BEFORE_20260716_K`. Campus 11 contained 1,000 ACTIVE members and a fresh 34 polls/34,000 responses. COFFEE sequential readiness passed from `01:10:57.982Z` to `01:11:39.051Z`, with quiet start `01:11:08.215Z`, nine polls, one reset, and headroom `nMod=0`, `reltuples=174011`, `triggerAt=17452`, expected/projected writes `11000/11000`, sufficient true. Its warmup once and ten measured requests completed with failure 0 and status 0. Rejected observed avg/p50/p95/p99/max were `877.7962/833.7495/1027.62265/1051.42693/1057.378ms`, count 10, rate `1.138697967/s`.

MEAL sequential readiness then timed out from `01:11:55.984Z` to `01:15:01.008Z`: quiet start null, 37 polls/resets, `nMod=11000`, `reltuples=174011`, `triggerAt=17452`, expected writes `11000`, projected `22000`, insufficient. Current `n_mod_since_analyze=11000` remained below the natural trigger and the last autoanalyze stayed `00:29:02.139845Z` / count 5, while the next projected mode exceeded the trigger. Partial state was COFFEE CLOSED 11/OPEN 6, MEAL CLOSED 17/OPEN 0, COFFEE charges 11,000, and MEAL charges 0. Adoption remained rejected with accepted/validated 0. `_K` is preserved and never reused; its numbers are neither a baseline nor an achievement. This deadlock led to the approved four-fresh-run single-mode protocol above; only the PM may perform those actual runs.

The PM-only first split attempt `_L` used `PERFORMANCE_192_BEFORE_CS_L`, `BEFORE_CS_L`, and `EXEC192_BEFORE_CS_L` with `MODE=coffee-sequential`. Runner preflight stopped before Docker, fixture, DB, or k6 work because the Node test process inherited `MODE` and the global DB collector fake child inherited it again. No report or fixture write was expected. These IDs remain held for PM artifact audit and are not reused. The global collector test harness now removes only an inherited parent `MODE`; explicit mode-scope evidence remains strict. Focused RED was 0/2 and GREEN was 2/2, while the full suite passes 77/77 under the same parent `MODE`.

## Latest-develop scenario compatibility (2026-07-16)

The scenario was rebased as test/scenario code onto `origin/develop` `6796ed146244d8f3f5b5dd7048ebe16865084a97` without modifying the validated before bundle or the production optimization branch. The #206 stable paging contract is now mirrored by the correctness verifier with `created_at DESC, id DESC`, matching the production same-direction ID tie-break. The #202 RLS change remains outside this direct JDBC observer path: database user/name come from the runtime target contract, and the runner does not use Supabase Data API credentials.

`TARGET_CONTRACT` is runtime-required and has no runner default. The checked-in `target-contract.json` is only the immutable historical before-target reference and must not be silently reused for an integration-after server. `compare-bundles.mjs` requires the same target role and workload contract, distinct source commits and immutable target identities, exact four-mode sets, validated conditional evidence, and preserves every mode's before/after metrics separately without producing a combined latency.

Status is **scenario-ready / not measured on latest develop**. This correction ran test-only RED `10/13`, focused GREEN `15/15`, and the complete issue-local static/fake suite `82/82`; all 31 JavaScript module syntax checks, Bash syntax, JSON parsing, and diff checks passed. No Docker, database, HTTP, fixture seed, k6 load, cleanup, or EXPLAIN command was executed. Test/scenario corrections may proceed in parallel across performance issues, but PM-owned actual loads remain strictly sequential on one server.

The PM-only retry `_CS_M` used `PERFORMANCE_192_BEFORE_CS_M`, `BEFORE_CS_M`, and `EXEC192_BEFORE_CS_M`. Preflight passed 77/77, but the actual runner stopped at the initial global DB capture because the exported runtime `MODE` still reached the production global collector call. The partial report is preserved; fixture seed/write and k6 load are expected to be zero, and these IDs are never reused. The runner now invokes only global DB capture and global DB validation through `env -u MODE`; mode-scope capture and validation still receive the exact selected mode.

The PM-only fresh `_CS_N` run matched target/source commit `355f79d`, passed preflight 77/77, and created the fresh 1,000-ACTIVE-member, 34-poll, 34,000-response fixture. It then stopped on entry to `run_mode` because one `local` statement referenced lowercase `mode` while assigning it under `set -u`. Warmup/measured k6 artifacts are zero and seed-report charge before/after change is zero. `_CS_N` is a fixture-write partial rejected run, is preserved, and is never reused. The actual function harness reproduced the nounset message with observed status 0 and no adoption record; splitting mode assignment from mode-dependent paths removes that fatal path, while controlled failures still exit 1 and write rejected adoption, so cleanup semantics were not otherwise changed.

This branch does not run an after measurement or build an issue-specific Docker image. After measurements belong to the final PM integration branch.
